# Fix Profile overflow + pause buttons + Help + scene doors

## 1. Profile text overflow
File: `Scripts/UI/ProfilePanel.cs`
- Card 440×360, title 28, body 14–18
- Auto-size TMP + ellipsis + RectMask2D
- Layout dari atas (PlaceTop) biar tidak keluar card

## 2. Pause/MainMenu/Exit tidak bisa ditekan
Akar: `GamePlaySystem.prefab` OnClick `m_Target: {fileID: 0}` (null) di Resume/MainMenu/Exit.
Fix: `PauseMenu.WirePauseButtons()` runtime `RemoveAllListeners` + bind ke `ResumeGame` / `BackToMainMenu` / `ExitGame` by name.

## 3. Help panel
- Baru: `Scripts/UI/HelpPanel.cs`
- `MainMenuController.OpenHelp()` buka Help (bukan Profile)
- Setting tetap buka Profile

## 4. Pintu antar-scene
Scenes di build: MainMenu, campusyard, classroom, Leaderboard, MainScene.
- `SceneDoor.cs` — trigger + tekan E → LoadScene
- `SceneDoorPromptUI.cs` — prompt `[E] Masuk ...`
- `SceneDoorBootstrap.cs` — auto spawn:
  - **campusyard**: Door → classroom, Door → MainScene (dekat player)
  - **classroom** / **MainScene**: Door return → campusyard
- Marker cube emas biar kelihatan

## File
- Edit: ProfilePanel, MainMenuController, PauseMenu
- Baru: HelpPanel, SceneDoor, SceneDoorPromptUI, SceneDoorBootstrap
- SceneDoor.Configure() public setter biar bootstrap isi field