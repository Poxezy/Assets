# Setup Scene Project MetaEdu — 5 Tahap

## Masalah Kritis
1. Unknown script GUID `59f81469` di Menupanel MainMenu → error MissingScript
2. Dua ScoreManager: `ScoreManager.cs` (root, Score/Level) vs `Core/ScoreManager.cs` (currentXP/totalScore) → harus pilih satu
3. Tiga movement script: `FPSController.cs`, `PlayerMovement.cs`, `SimplePlayerMovement.cs` → harus pilih satu
4. Leaderboard & MainScene tidak punya manager systems
5. MainScene punya dua Capsule (FPSController + PlayerMovement)

---

## Tahap 1: Bersihkan Konflik (P1)
- Hapus unknown script dari Menupanel MainMenu.unity
- **Pakai `ScoreManager.cs` (root)** — nonaktifkan `Core/ScoreManager.cs` via rename ke `.cs.bak`
- **Pakai `Player/FPSController.cs` + `Player.prefab`** — nonaktifkan `PlayerMovement.cs` dan `SimplePlayerMovement.cs`
- MainScene: hapus Capsule (PlayerMovement), pertahankan Capsule (1) dgn FPSController

## Tahap 2: Buat GameManagers Prefab (P1)
Satu prefab berisi 11 singleton untuk di-drop ke scene:
- GameManager, ScoreManager, DatabaseManager, SceneLoader, AudioManager (+2 AudioSource child), SaveLoadManager, DialogueManager, QuestManager, QuizManager, MiniGameManager, InteractionManager

## Tahap 3: Setup per Scene (P2)
- **MainMenu**: wire button onClick (Start, Leaderboard, Exit), hapus unknown script, drop GameManagers
- **campusyard**: hapus ScoreManager/DatabaseManager standalone, drop Player.prefab + GameManagers, tambah Doorlab Variant + KnowledgeItem + NPC
- **classroom**: hapus standalone managers, drop Player + GameManagers, assign layer Interactable
- **Leaderboard**: drop GameManagers (scene kosong tanpa manager)
- **MainScene**: hapus Capsule duplicate, drop GameManagers + GamePlaySystem + EventSystem

## Tahap 4: ScriptableObjects (P3)
Buat QuizData, DialogueData, QuestData assets untuk sistem kuis, dialog, quest

## Tahap 5: Build Settings & Layer (P3)
- Scenes in Build: MainMenu, campusyard, classroom, Leaderboard
- Buat layer "Interactable" (Layer 8)
- Disable Dynamic Batching

---

Yang dikerjakan via code: edit YAML scene, rename/disable script. Sisanya drag-drop di Unity Editor.